#include <simulator/robots/control/PIDLoop.h>



/**
 * @brief takes the current value and computes a response value based on it.
 * 
 * @param current the current value of the what the control loop is driving
 * @return double the response to the current value 
 */
double PIDLoop::Update(double current) {
    /* to get the current time you can use 
     * auto now = std::chrono::system_clock::now(); 

     * so the difference in time would be double dt = now - _lastTime; 

    */



   return 0; 
}