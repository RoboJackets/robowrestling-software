#ifndef WORLD_ENUM_H
#define WORLD_ENUM_U

enum line_sensor {
    LEFT_ON,
    RIGHT_ON,
    BOTH_ON,
    OFF
};

enum enemy_position {
    LEFT,
    FRONT_LEFT,
    FRONT,
    FRONT_RIGHT,
    RIGHT,
    CLOSE_LEFT,
    CLOSE_MID_LEFT,
    CLOSE_MID_RIGHT,
    CLOSE_RIGHT
}

#endif